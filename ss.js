// const text = require('fs').readFileSync('list.json').toString();

// const obj = JSON.parse(text);

// Object.keys(obj).forEach((el, idx) => {
// 	obj[el] = [obj[el], idx + 1];
// });

// console.log(obj);

// // const result = {};

// // obj.forEach(el => {
// // 	result[el[0]] = [el[1], el[2]];
// // });

// require('fs').writeFile('list.json', JSON.stringify(obj), err => {
// 	console.log(err);
// });
